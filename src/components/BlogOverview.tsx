interface BOProps {
    nodes: { [key: string]: string; }
}

export default function BlogOverview(props: BOProps): JSX.Element {
    return(
        <div className="blog-view">
        </div>
    );
}
